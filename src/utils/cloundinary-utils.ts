import cloudinary from "../middleware/cloudinary"

export const upload = async(file:Express.Multer.File, dir = '/')=>{
    try {
        const upload_dir = 'team ankit ' +dir
    } catch (error) {
        
    }
}